import math
import random

class inverOver:
    def inverOver(pops):
        # the condition of while loop -> not satified termination-condition
        while True:
            # pops -> the population P; item -> individual
            for item in pops:
                individual = item
                c = individual[random.random(len(individual) - 1)]
                for item in pops:
                    if random() <= p
                        # select the city c1 from the remaining cities
                        c1 = individual[random.random(len(individual) - 1 - c)]
                    else:
                        v = individual[random.random(len(individual) - 1)]
                        # assign to c' the 'next' city to the city c in the selected individual
                        c1 assign c
                    if nextCity in c
                        break
                    # inverse the section from the next city of city c to the city c' in S'
                    citylist = nextCity
                    list(reversed(citylist))
                    c = c1
                # if eval(S') <= eval(s')
                if (eval())
                    # S' = s'
                    xxxxx











